Put the three section images here before deploying:

  scoring.png   - category weights graphic ("What each category shows" section)
  research.png  - Measure / Score / Become Cited graphic ("How GEO score is calculated")
  femida.png    - robot justice statue ("What Snoika's GEO checker shows..." section)

Use exactly these filenames. Then run: npx wrangler deploy
(If you prefer Cloudinary URLs instead, replace the three img/*.png src values in public/index.html.)
